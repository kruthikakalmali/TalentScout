import React from 'react';
import { Box, Text } from '@chakra-ui/react';

interface GeneratedReportCandidateProps {
}

const GeneratedReportCandidate: React.FC<GeneratedReportCandidateProps> = () => (
  <Box
    mb={6}
    p={6}
    bg="#2d3748"
    borderRadius="md"
    boxShadow="sm"
    w="100%"
  >
  </Box>
);

export default React.memo(GeneratedReportCandidate);